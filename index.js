/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LearningStyling from './learning_styling';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LearningStyling);
